apiVersion: v1
kind: Service
metadata:
  name: unonpservice-srv
spec:
  type: NodePort
  selector:
    app: unoservice
  ports:
    - name: unoservice
      protocol: TCP
      port: 80
      targetPort: 8080
